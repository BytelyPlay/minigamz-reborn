syntax = "proto3";
package org.minigamzreborn.bytelyplay.protobuffer.packets;
import "enums/ServerType.proto";

message RegisterServerPacketC2S {
  // Why do i have to specify the full thing?
  org.minigamzreborn.bytelyplay.protobuffer.enums.ServerType type = 1;
  string address = 2;
  uint32 port = 3;
}